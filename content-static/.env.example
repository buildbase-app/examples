# Only the build needs these, never the site. In CI, keep them as secrets.
# An org API token with the content-reader role: <orgId>:<secret>
BUILDBASE_API_TOKEN=
# Optional
# BUILDBASE_SERVER_URL=https://api.console.buildbase.app
# CONTENT_COLLECTIONS=release-notes
